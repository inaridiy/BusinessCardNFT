import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className="w-full">
      <div className="h-64 bg-gradient-to-tr from-primary to-secondary" />
    </div>
  );
};

export default Home;
